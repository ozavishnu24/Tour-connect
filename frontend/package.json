{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "serve -s public -l 3001",
    "dev": "serve -s public -l 3001",
    "build": "echo 'No build step required'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Frontend for Tour Connect",
  "devDependencies": {
    "serve": "^14.2.5"
  }
}